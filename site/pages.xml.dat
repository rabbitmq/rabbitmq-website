<?xml version="1.0" encoding="utf-8"?>
<x:pages xmlns:x="http://www.rabbitmq.com/2011/extensions">
  <!--
      TODO rewrite

      ~~~ Using this file ~~~
      The purpose of this file is to provide a central record of
      "related links" displayed on each page, the aim being to
      simplify maintenance.

      For each page where you want to display related links, create
      an <x:page> section with a key attribute that is unique within
      the set of all x:page elements. A suggestion is to use the page's
      url (relative to the root of the site) as the key attribute.

      By default, the link text and a tooltip are generated from the
      target page's title. To override this behaviour you can specify
      on x:page an optional text attribute containing the link text
      and/or an optional tooltip attribute containing the tooltip text.

      Add a child <x:related> element for each link that you want to
      appear in the related links section. Use the page's relative url
      as the url attribute.

      In the page that you wish to insert the related links add a
      <x:related-links> element. You must specify a key attribute that
      matches the key that you used for the <x:page> section in this
      file.

  -->

  <!-- TODO obvs this is rather incomplete -->
   <x:page key="download" text="Download">
     <x:page key="server" text="Server Downloads"/>
     <x:page key="java-client" text="Java Client Downloads"/>
     <x:page key="dotnet" text=".NET Client Downloads"/>
     <x:page key="download-ex" text="Experimental Downloads"/>
   </x:page>
   <x:page key="documentation" text="Documentation">
     <x:page key="install" text="Server Documentation">
       <x:page key="authentication" text="Authentication"/>
       <x:page key="configure" text="Configuration">
         <x:related url="plugins"/>
       </x:page>
     </x:page>
     <x:page key="clients" text="Client Documentation">
       <x:page key="api-guide" text="Java Client API Guide"/>
     </x:page>
   </x:page>
   <x:page key="getstarted" text="Get Started"/>
   <x:page key="services" text="Services"/>
   <x:page key="contact" text="Community"/>
</x:pages>
