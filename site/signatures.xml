<?xml-stylesheet type="text/xml" href="page.xsl"?>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:doc="http://www.rabbitmq.com/namespaces/ad-hoc/doc">
  <head>
    <title>RabbitMQ - Signatures</title>
  </head>
  <body>
    <doc:div>
      <p class="intro">
	This page describes the signing process we use when building
	RabbitMQ release packages, and how to verify the signatures on
	packages you download.
      </p>

      <doc:section name="signing-process">
	<doc:heading>Our signing process</doc:heading>
	<p>
	  When we build package archive files, both binary and source,
	  we digitally sign them using <a
	  href="http://www.gnupg.org/">GnuPG</a> and <a
	  href="rabbitmq-signing-key-public.asc">our public signing
	  key</a>.
	</p>
      </doc:section>

      <doc:section name="checking-signatures">
	<doc:heading>Checking signatures</doc:heading>
	<p>
	  To check the signatures, use <a
	  href="rabbitmq-signing-key-public.asc">our public signing
	  key</a> with any <a
	  href="http://www.openpgp.org/">OpenPGP</a>-compatible
	  implementation. For instance, using <a
	  href="http://www.gnupg.org/">GnuPG</a>, the appropriate
	  command for checking a detached signature is
	</p>
	<pre class="sourcecode">gpg --verify <i>filename</i>.asc <i>filename</i></pre>
	<p>
	  Here's an example session, after having retrieved a RabbitMQ
	  source archive and its associated detached signature from
	  the download area:
	</p>
	<pre class="sourcecode">$ gpg --verify rabbitmq-0.0.tar.gz.asc rabbitmq-0.0.tar.gz
gpg: Signature made Mon 29 Jan 2007 05:33:27 PM GMT using DSA key ID E96F1FA7
gpg: Good signature from "RabbitMQ Signing Key &lt;packaging@rabbitmq.com&gt;"</pre>
	<p>
	  If the signature is valid, you will expect a "Good
	  signature" message; if it is invalid, you will see a "BAD
	  signature" message.
	</p>
      </doc:section>
    </doc:div>
  </body>
</html>
